---
phase: 01-monorepo-foundation
task: 0
total_tasks: 6
status: planned_not_started
last_updated: 2026-03-22T11:01:16.239Z
---

<current_state>
Phase 1 planning is 100% complete. Nothing has been executed yet. The project directory exists at /Users/merlinzuni/develop but is NOT a git repo (no commits, all .planning files are untracked). Phase 1 execution will scaffold the entire monorepo including git init.
</current_state>

<completed_work>

**This session (setup):**
- PROJECT.md — full project context written
- REQUIREMENTS.md — 27 v1 requirements across 6 categories (INFRA, TOKEN, FIGMA, STORY)
- ROADMAP.md — 6-phase roadmap with tasks and verification criteria
- STATE.md — project state file
- config.json — workflow config
- Domain research (4 parallel agents): TOKENS.md, FIGMA.md, STORYBOOK.md, STRUCTURE.md
- SUMMARY.md — research synthesis
- Phase 1 research: 01-RESEARCH.md (versions verified live against npm registry)
- Phase 1 plans: 01-01-PLAN.md (Wave 1), 01-02-PLAN.md (Wave 2)
- Phase 1 validation: 01-VALIDATION.md (Nyquist compliance, smoke check script)
- Fixed INFRA-02 requirement mapping (CSS output deferred to Phase 2)
- Figma MCP added to Claude Code user config (localhost:3845, SSE, ✓ Connected)

**Phase 1 tasks — none started:**
- Wave 1 (01-01): Root workspace + tokens + primitives packages
- Wave 2 (01-02): Storybook + ESLint + Changesets
</completed_work>

<remaining_work>

**Wave 1 — Plan 01-01:**
- Task 1: Root workspace init — package.json (workspaces), turbo.json (v2 `tasks` key), tsconfig.base.json (strict), .gitignore
- Task 2: Scaffold packages/tokens — package.json, tsconfig.json, tsup.config.ts (dts:true, dual ESM+CJS), src/index.ts (empty barrel)
- Task 3: Scaffold packages/primitives — same structure, add @design-system-x/tokens:* dep, run npm install, verify turbo run build

**Wave 2 — Plan 01-02 (after Wave 1):**
- Task 1: ESLint 9 flat config — eslint.config.mjs using typescript-eslint v8 single package, projectService:true
- Task 2: Storybook workspace — apps/storybook/, run npx storybook@latest init inside it
- Task 3: Changesets — changeset init, set access:"public" in .changeset/config.json, full phase gate verification
</remaining_work>

<decisions_made>

- **Turborepo v2**: use `tasks` key, NOT `pipeline` — v1 syntax still works but emits deprecation warnings
- **tsup + TypeScript**: do NOT set `composite: true`; tsup uses esbuild, not tsc for transpilation; use `dts: true` for type generation; use npm workspace symlinks (not TS project references) for cross-package resolution
- **ESLint**: use single `typescript-eslint` package (v8+) — replaces old plugin+parser pair; `projectService: true` auto-discovers tsconfig files
- **Changesets**: default `access: "restricted"` must be changed to `"public"` before first publish — npm returns 402 otherwise
- **Storybook**: use `npx storybook@latest init` — do NOT manually pin versions (CLI at 10.3.1 vs addons at 8.6.14 appear mismatched)
- **Turborepo + Storybook**: dev task needs `persistent: true` + `cache: false` or Turborepo kills the dev server immediately
- **INFRA-02**: CSS output deferred to Phase 2 (Style Dictionary); Phase 1 only produces TS/ESM/CJS from tsup
- **Figma MCP**: connected at localhost:3845 via SSE in Figma desktop app; added to ~/.claude.json
</decisions_made>

<blockers>

None — all planning blockers resolved. Ready to execute.
</blockers>

<context>
Full project initialization session. User is building "Design System X" — a full design system for a team with 3-tier token architecture (Primitive → Semantic → Component), category.property.modifier naming, Figma Variables → Tokens Studio Pro → DTCG JSON → Style Dictionary v4 → CSS + TypeScript pipeline, multi-brand + light/dark theming, Storybook 8 docs. Figma Organization plan (40 modes). Composite tokens (typography, elevation) go through Tokens Studio directly (not Figma Variables).

The project directory /Users/merlinzuni/develop is NOT a git repo yet. Phase 1 execution will create the monorepo structure. Figma MCP is connected and will be available in the new session (localhost:3845 green dot in Figma Dev Mode panel).
</context>

<next_action>
Start a new Claude session (`/clear`), then run:

/gsd:execute-phase 1

The executor will scaffold the full monorepo in 2 waves. No human actions needed before starting — all decisions are locked in the plans.
</next_action>
