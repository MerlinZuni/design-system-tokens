---
phase: 06-primitive-components-figma-integration
task: UAT
total_tasks: 10
status: in_progress
last_updated: 2026-03-23T22:09:23.067Z
---

<current_state>
Phase 6 execution is complete — all 3 plans shipped (06-01, 06-02, 06-03). Verification passed 12/12 must-haves. Human UAT is in progress with 4 items. Extended visual polish session addressed ~15 Storybook styling issues based on user's design review.
</current_state>

<completed_work>

- Plan 06-01: 6 primitive components (Text, ColorSwatch, Surface, Stack, Inline, VisuallyHidden) + Phase 5 gap fixes (BREAKPOINTS import, StylesTokenTable)
- Plan 06-02: 6 .stories.tsx + 6 .mdx docs files for all primitives
- Plan 06-03: @figma/code-connect installed, 6 .figma.tsx files with placeholder node IDs, RELEASE_CHECKLIST.md
- Verification: 12/12 must-haves passed, 06-HUMAN-UAT.md created
- Fix: Figma import path (storybook-addon-designs → @storybook/addon-designs)
- Fix: Removed autodocs tag conflict (Storybook 10 errors with both MDX and autodocs)
- Fix: Theme switching on docs-only pages (initial data attributes + StylesTokenTable useEffect)
- Fix: Dark mode text color (added color: text-default to decorator)
- Fix: Loaded Archivo from Google Fonts, set font-family on decorator
- Fix: Docs page headers styled with Archivo Black (900) matching Figma
- Fix: Consistent code tag styling across all components (grey box + border)
- Fix: Copy-to-clipboard SVG icons → Material Symbols icons (content_copy/check)
- Fix: Removed zebra stripes from token tables
- Fix: Increased color swatch preview size (24px → 40px)
- Fix: SpacingBar token styling aligned with standard code tags
- Fix: Google Sans Code as monospace font site-wide
- UAT item 2 confirmed: Figma embed block renders with placeholder URL
</completed_work>

<remaining_work>

- Human UAT item 1: Autodocs props table rendering (navigate to Primitives/Text Docs tab)
- Human UAT item 3: Theme-reactive Styles pages (need Storybook restart to test after fixes)
- Human UAT item 4: All 6 components visible in Primitives sidebar group
- Mark phase complete + update roadmap (after UAT passes)
</remaining_work>

<decisions_made>

- Removed `tags: ['autodocs']` because Storybook 10 errors when CSF has both autodocs tag and matching MDX `<Meta of={}>`
- Fixed Figma import to `@storybook/addon-designs/blocks` — the installed package, not `storybook-addon-designs`
- Used Archivo Black (weight 900) for H1/H2 headers — matches Figma pages but 900 isn't in the token system (possibly from external Liip Slides:Base library in Figma)
- `StylesTokenTable` syncs `data-brand`/`data-theme` via useEffect since the decorator only fires on story renders, not pure MDX pages
- Google Sans Code replaces system monospace for all code/token displays
- Material Symbols Outlined for copy icons (content_copy → check on click)
</decisions_made>

<blockers>
- None blocking execution. Figma node ID placeholders (TODO-NODE-ID) need manual replacement before Code Connect publish, but this is gated in RELEASE_CHECKLIST.md.
</blockers>

<context>
User has expert design eye and caught many visual inconsistencies during UAT (font mismatches, color mismatches, sizing issues, accessibility concerns). Expect further visual refinements when resuming. The code is functionally complete — remaining work is visual polish and UAT sign-off.

Key Storybook 10 gotchas discovered:
1. Docs pages don't run decorators for inline MDX content (only for Canvas-rendered stories)
2. `@storybook/preview-api` can't be directly imported in preview.tsx (fails Vite resolve) — use try/catch require() pattern
3. autodocs tag + MDX Meta is a hard error, not a warning
</context>

<next_action>
Start with: Restart Storybook dev server, then complete remaining 3 human UAT items. After all pass, run the phase completion workflow (update_roadmap step).
</next_action>
