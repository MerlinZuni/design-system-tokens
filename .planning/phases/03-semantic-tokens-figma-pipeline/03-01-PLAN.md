---
phase: 03-semantic-tokens-figma-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/tokens/tokens/primitives/color.tokens.json
  - packages/tokens/tokens/semantic/parent-brand/light.tokens.json
  - packages/tokens/tokens/semantic/parent-brand/dark.tokens.json
  - packages/tokens/tokens/semantic/child-brand/light.tokens.json
  - packages/tokens/tokens/semantic/child-brand/dark.tokens.json
  - packages/tokens/tokens/$themes.json
  - packages/tokens/style-dictionary.config.mjs
  - packages/tokens/package.json
  - turbo.json
autonomous: true
requirements: [TOKEN-06, TOKEN-07, TOKEN-08, TOKEN-10]

must_haves:
  truths:
    - "4 semantic CSS files exist at dist/parent-brand/light.css, dist/parent-brand/dark.css, dist/child-brand/light.css, dist/child-brand/dark.css"
    - "Semantic CSS files contain only semantic token vars (color.background.*, color.text.*, etc.), not primitive token vars"
    - "All semantic token $value fields are aliases ({color.brand.500} syntax), never raw hex values"
    - "Switching which CSS file is loaded changes all semantic tokens from light to dark or brand to brand"
    - "Primitive CSS (dist/css/tokens.css) still builds correctly alongside semantic outputs"
    - "$themes.json maps 4 theme entries for Tokens Studio Pro export"
  artifacts:
    - path: "packages/tokens/tokens/semantic/parent-brand/light.tokens.json"
      provides: "Semantic light mode aliases for parent-brand"
      contains: "color.brand.500"
    - path: "packages/tokens/tokens/semantic/parent-brand/dark.tokens.json"
      provides: "Semantic dark mode aliases for parent-brand"
      contains: "color.brand.400"
    - path: "packages/tokens/tokens/semantic/child-brand/light.tokens.json"
      provides: "Semantic light mode aliases for child-brand"
      contains: "color.slate.500"
    - path: "packages/tokens/tokens/semantic/child-brand/dark.tokens.json"
      provides: "Semantic dark mode aliases for child-brand"
      contains: "color.neutral.950"
    - path: "packages/tokens/style-dictionary.config.mjs"
      provides: "Multi-instance SD loop for brand x mode"
      contains: "for (const brand of BRANDS)"
    - path: "packages/tokens/tokens/$themes.json"
      provides: "Tokens Studio Pro theme configuration"
      contains: "selectedTokenSets"
  key_links:
    - from: "packages/tokens/tokens/semantic/parent-brand/light.tokens.json"
      to: "packages/tokens/tokens/primitives/color.tokens.json"
      via: "DTCG alias references"
      pattern: "\\{color\\.(brand|neutral|secondary)\\."
    - from: "packages/tokens/style-dictionary.config.mjs"
      to: "packages/tokens/tokens/semantic/"
      via: "source array in SD loop"
      pattern: "tokens/semantic/\\$\\{brand\\}/\\$\\{mode\\}"
    - from: "packages/tokens/package.json"
      to: "packages/tokens/dist/parent-brand/"
      via: "exports map"
      pattern: "parent-brand/light"
---

<objective>
Author all Tier 2 semantic tokens (4 brand x mode JSON files), update primitive color scales, refactor Style Dictionary to multi-instance loop, and wire package exports for multi-theme CSS output.

Purpose: This is the code bootstrap step (per D-15). All semantic JSON files are authored in code first, validated via SD build, then pushed to Figma in Plan 02. After this plan, the project has a complete multi-brand, multi-mode token system producing 4 theme CSS files.

Output: 4 semantic token JSON files, updated SD config with multi-instance loop, $themes.json for Tokens Studio, 4 CSS output files in dist/, updated package.json exports and turbo.json outputs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-primitive-token-pipeline/02-02-SUMMARY.md
@.planning/phases/03-semantic-tokens-figma-pipeline/03-RESEARCH.md

<interfaces>
<!-- Existing primitive token naming conventions (from Phase 2) -->

From packages/tokens/tokens/primitives/color.tokens.json:
- color.brand.{50-950} — currently Tailwind violet placeholder, will be replaced with teal scale
- color.neutral.{0,50-950,1000} — Tailwind zinc, #ffffff to #000000
- color.red/green/yellow/blue/orange/purple.{50-950} — Tailwind v3 interface hues

From packages/tokens/tokens/primitives/spacing.tokens.json:
- spacing.{0,0.5,1,...,96} — Tailwind v3 spacing scale, px values

From packages/tokens/tokens/primitives/elevation.tokens.json:
- elevation.{none,sm,md,lg,xl} — DTCG shadow type

From packages/tokens/style-dictionary.config.mjs:
- Single-instance SD config with register(StyleDictionary), preprocessors: ['tokens-studio'], expand: { typesMap: expandTypesMap }
- CSS platform: transformGroup 'tokens-studio', transforms ['name/kebab'], prefix 'dsx', outputReferences: true
- TS platform: breakpoints only

From packages/tokens/package.json:
- exports: { ".": {...}, "./css": "./dist/css/tokens.css" }
- scripts: { "build:tokens": "node style-dictionary.config.mjs", "build": "tsup" }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update primitive color scales and author 4 semantic token JSON files with $themes.json</name>
  <files>
    packages/tokens/tokens/primitives/color.tokens.json,
    packages/tokens/tokens/semantic/parent-brand/light.tokens.json,
    packages/tokens/tokens/semantic/parent-brand/dark.tokens.json,
    packages/tokens/tokens/semantic/child-brand/light.tokens.json,
    packages/tokens/tokens/semantic/child-brand/dark.tokens.json,
    packages/tokens/tokens/$themes.json
  </files>
  <read_first>
    packages/tokens/tokens/primitives/color.tokens.json,
    packages/tokens/tokens/primitives/spacing.tokens.json,
    packages/tokens/tokens/primitives/elevation.tokens.json,
    .planning/phases/03-semantic-tokens-figma-pipeline/03-RESEARCH.md
  </read_first>
  <action>
**Step 1: Update primitive color scales in color.tokens.json (per D-03, D-04)**

Replace the `color.brand` group (currently Tailwind violet placeholder) with a teal scale anchored at `#4FC4C4` (step 500). Generate the full 50-950 scale using perceptual lightness methodology. Use these exact values:

```json
"brand": {
  "50":  { "$value": "#f0fdfd" },
  "100": { "$value": "#ccfbf1" },
  "200": { "$value": "#99f6e4" },
  "300": { "$value": "#7de8d6" },
  "400": { "$value": "#5dd8c8" },
  "500": { "$value": "#4FC4C4" },
  "600": { "$value": "#2da3a3" },
  "700": { "$value": "#1a7f7f" },
  "800": { "$value": "#115e5e" },
  "900": { "$value": "#0a4040" },
  "950": { "$value": "#052525" }
}
```

Add a new `secondary` group (purple scale, per D-03, base `#5944af` at 500):

```json
"secondary": {
  "50":  { "$value": "#f5f3ff" },
  "100": { "$value": "#ede9fe" },
  "200": { "$value": "#ddd6fe" },
  "300": { "$value": "#c4b5fd" },
  "400": { "$value": "#a78bfa" },
  "500": { "$value": "#5944af" },
  "600": { "$value": "#4c3a99" },
  "700": { "$value": "#3f2f83" },
  "800": { "$value": "#32256d" },
  "900": { "$value": "#261c57" },
  "950": { "$value": "#1a1240" }
}
```

Add a `slate` group (5-step scale for child-brand, per D-04, base `#1C1C28` at 500):

```json
"slate": {
  "100": { "$value": "#c8c8d0" },
  "300": { "$value": "#6e6e80" },
  "500": { "$value": "#1C1C28" },
  "700": { "$value": "#12121a" },
  "900": { "$value": "#0a0a10" }
}
```

Update the `$description` inside the `color` group to note the teal brand scale and added secondary/slate scales.

**Step 2: Author 4 semantic token JSON files**

Create directory structure: `packages/tokens/tokens/semantic/parent-brand/` and `packages/tokens/tokens/semantic/child-brand/`.

Each file follows the DTCG format with `$value` aliases (curly brace references like `{color.brand.500}`). NO raw hex values in semantic files — all must be aliases to primitives.

Include ALL categories from D-07 (color semantic vocabulary) and D-08 (spacing semantic vocabulary) in each file. The `space` block is identical across all 4 files (per Research Option A — duplicate for simplicity).

Use the full canonical file shape from 03-RESEARCH.md Pattern 1 as the template. The complete semantic color categories are:
- `color.background` (default, subtle, surface, overlay, inverse)
- `color.surface` (card, panel, popover, tooltip, sunken)
- `color.text` (default, muted, subtle, inverse, disabled, link, link-hover, link-visited)
- `color.action` (default, hover, pressed, disabled, subtle)
- `color.focus` (ring)
- `color.border` (default, subtle, strong, inverse, focus)
- `color.divider` (default)
- `color.icon` (default, muted, inverse, disabled, action)
- `color.status.error` (background, text, border, icon)
- `color.status.warning` (background, text, border, icon)
- `color.status.success` (background, text, border, icon)
- `color.status.info` (background, text, border, icon)
- `color.overlay` (scrim)
- `color.skeleton` (base, highlight)
- `color.highlight` (selected)
- `shadow` (sm, md, lg) — aliasing elevation primitives
- `space.component` (xs, sm, md, lg, xl) — aliasing spacing primitives
- `space.layout` (xs, sm, md, lg, xl) — aliasing spacing primitives
- `space.inline` (xs, sm, md, lg) — aliasing spacing primitives

**Per-file alias strategies:**

**parent-brand/light.tokens.json:** Use the canonical light mode mapping from 03-RESEARCH.md Pattern 1 exactly. Key mappings:
- `color.background.default` -> `{color.neutral.0}` (white canvas)
- `color.text.default` -> `{color.neutral.900}` (near-black)
- `color.action.default` -> `{color.brand.500}` (teal)
- `color.action.hover` -> `{color.brand.600}`
- Status text uses step 700+ for WCAG AA on step 50 backgrounds (per Research Pitfall 7)

**parent-brand/dark.tokens.json:** Brand-tinted dark surfaces per D-10 (Vercel-inspired, MORE noticeable depth contrast). Key mappings:
- `color.background.default` -> `{color.brand.950}` (deepest teal-tinted)
- `color.background.subtle` -> `{color.brand.900}`
- `color.surface.card` -> `{color.brand.900}`
- `color.surface.panel` -> `{color.brand.800}`
- `color.surface.popover` -> `{color.brand.700}`
- `color.text.default` -> `{color.neutral.50}` (near-white)
- `color.action.default` -> `{color.brand.400}` (1 step lighter for dark-mode legibility)
- Status backgrounds use step 900+ for dark mode, status text uses step 200-300
- `color.border.default` -> `{color.neutral.700}`
- `color.skeleton.base` -> `{color.neutral.800}`

**child-brand/light.tokens.json:** Near-black slate primary, navy secondary per D-04. Key mappings:
- `color.background.default` -> `{color.neutral.0}` (white canvas)
- `color.text.default` -> `{color.slate.500}` (near-black slate for text)
- `color.action.default` -> `{color.secondary.500}` (navy/purple accent for action — NOT slate, per Research Pattern 3 note about near-black as poor action color)
- `color.action.hover` -> `{color.secondary.600}`
- `color.action.subtle` -> `{color.secondary.50}`
- `color.focus.ring` -> `{color.secondary.500}`
- `color.text.link` -> `{color.secondary.500}`
- Status tokens same as parent-brand light (red/green/yellow/blue are brand-independent)

**child-brand/dark.tokens.json:** Deep charcoal surfaces per D-11 (neutral, NOT tinted). Key mappings:
- `color.background.default` -> `{color.neutral.950}` (deepest neutral)
- `color.surface.card` -> `{color.neutral.900}`
- `color.surface.panel` -> `{color.neutral.800}`
- `color.surface.popover` -> `{color.neutral.700}`
- `color.text.default` -> `{color.neutral.50}`
- `color.action.default` -> `{color.secondary.400}` (lighter purple accent on dark)
- Status backgrounds use step 900+ for dark mode

CRITICAL per D-12: Verify all text/background pairings pass WCAG AA (4.5:1). Use these known-safe pairings:
- neutral.900 on neutral.0: ~21:1 (AAA)
- neutral.50 on neutral.950: ~19:1 (AAA)
- brand.500 (#4FC4C4) on neutral.0: verify >= 4.5:1 (teal on white may fail — if so, use brand.600 or brand.700 for text roles)
- neutral.900 on brand.500: verify >= 4.5:1 (per D-13, should pass AAA ~7:1)

**Step 3: Create $themes.json**

Create `packages/tokens/tokens/$themes.json` with 4 theme entries exactly as documented in 03-RESEARCH.md (the `$themes.json for Tokens Studio Pro` section). Each entry has:
- `name`: "light" or "dark"
- `group`: "parent-brand" or "child-brand"
- `selectedTokenSets`: primitives as "source", brand-mode semantic set as "enabled"

The paths in `selectedTokenSets` must match the file paths relative to the tokens directory WITHOUT the `.tokens.json` extension:
- `"primitives/color"` (not `"primitives/color.tokens.json"`)
- `"primitives/spacing"` (not `"primitives/spacing.tokens.json"`)
- `"primitives/typography"`
- `"primitives/elevation"`
- `"semantic/parent-brand/light"` (not with `.tokens.json`)
  </action>
  <verify>
    <automated>cd "/Users/merlinzuni/develop/design system tokens/packages/tokens" && test -f tokens/semantic/parent-brand/light.tokens.json && test -f tokens/semantic/parent-brand/dark.tokens.json && test -f tokens/semantic/child-brand/light.tokens.json && test -f tokens/semantic/child-brand/dark.tokens.json && test -f tokens/\$themes.json && grep -q "color.brand.500" tokens/semantic/parent-brand/light.tokens.json && grep -q "color.brand.400" tokens/semantic/parent-brand/dark.tokens.json && grep -q "color.secondary.500" tokens/semantic/child-brand/light.tokens.json && grep -q "color.neutral.950" tokens/semantic/child-brand/dark.tokens.json && grep -q "selectedTokenSets" tokens/\$themes.json && grep -q "#4FC4C4" tokens/primitives/color.tokens.json && grep -q "secondary" tokens/primitives/color.tokens.json && grep -q "slate" tokens/primitives/color.tokens.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - color.tokens.json contains `"brand"` group with `"500": { "$value": "#4FC4C4" }`
    - color.tokens.json contains `"secondary"` group with `"500": { "$value": "#5944af" }`
    - color.tokens.json contains `"slate"` group with `"500": { "$value": "#1C1C28" }`
    - All 4 semantic JSON files exist under tokens/semantic/{brand}/{mode}.tokens.json
    - Every `$value` in semantic files uses alias syntax `{...}` — zero raw hex values
    - Each semantic file contains color.background, color.surface, color.text, color.action, color.focus, color.border, color.divider, color.icon, color.status, color.overlay, color.skeleton, color.highlight, shadow, space.component, space.layout, space.inline
    - parent-brand dark uses brand-tinted surfaces (brand.950/900/800/700 for depth levels)
    - child-brand dark uses neutral surfaces (neutral.950/900/800/700 for depth levels)
    - $themes.json contains exactly 4 entries with groups "parent-brand" and "child-brand"
    - No `$description` at file root level in any semantic JSON file (SD collision bug)
  </acceptance_criteria>
  <done>All primitive color scales updated (brand=teal, secondary=purple, slate added). 4 semantic JSON files authored with complete vocabulary (D-07, D-08). $themes.json created for Tokens Studio Pro. Zero raw hex values in semantic files — all aliases.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Style Dictionary to multi-instance loop and update package exports</name>
  <files>
    packages/tokens/style-dictionary.config.mjs,
    packages/tokens/package.json,
    turbo.json
  </files>
  <read_first>
    packages/tokens/style-dictionary.config.mjs,
    packages/tokens/package.json,
    turbo.json,
    .planning/phases/03-semantic-tokens-figma-pipeline/03-RESEARCH.md
  </read_first>
  <action>
**Step 1: Refactor style-dictionary.config.mjs to multi-instance loop (TOKEN-10)**

Replace the current single-instance config with the multi-instance pattern from 03-RESEARCH.md Pattern 2. The new config must have:

1. Keep the existing imports at top:
```javascript
import StyleDictionary from 'style-dictionary';
import { register, expandTypesMap } from '@tokens-studio/sd-transforms';
register(StyleDictionary);
```

2. Define brand/mode arrays:
```javascript
const BRANDS = ['parent-brand', 'child-brand'];
const MODES = ['light', 'dark'];
```

3. **Primitives instance (unchanged from Phase 2):** Keep the existing `sdPrimitives` instance that:
   - `source: ['tokens/primitives/**/*.tokens.json']`
   - `preprocessors: ['tokens-studio']`
   - `expand: { typesMap: expandTypesMap }`
   - CSS platform: `transformGroup: 'tokens-studio'`, `transforms: ['name/kebab']`, `prefix: 'dsx'`, `buildPath: 'dist/css/'`, destination `'tokens.css'`, format `'css/variables'`, `outputReferences: true`, `selector: ':root'`, filter excluding breakpoints
   - TS platform: breakpoints only to `src/breakpoints.ts`

4. **Semantic instances (NEW — nested loop):** For each brand x mode combination:
   - `include: ['tokens/primitives/**/*.tokens.json']` — loaded for alias resolution, NOT output
   - `source: ['tokens/semantic/${brand}/${mode}.tokens.json']` — ONLY semantic tokens output
   - `preprocessors: ['tokens-studio']`
   - `expand: { typesMap: expandTypesMap }`
   - CSS platform only (no TS platform for semantics):
     - `transformGroup: 'tokens-studio'`
     - `transforms: ['name/kebab']`
     - `prefix: 'dsx'`
     - `buildPath: 'dist/${brand}/'`
     - destination: `'${mode}.css'`
     - format: `'css/variables'`
     - `outputReferences: true`
     - selector: `mode === 'light' ? ':root' : '[data-theme="dark"]'`

5. Execution order — sequential per instance, clean before build:
```javascript
// Primitives first
await sdPrimitives.cleanAllPlatforms();
await sdPrimitives.buildAllPlatforms();

// Then semantic instances
for (const brand of BRANDS) {
  for (const mode of MODES) {
    // ... create SD instance ...
    await sd.cleanAllPlatforms();
    await sd.buildAllPlatforms();
  }
}
```

CRITICAL: Do NOT use `Promise.all()` for the loop — sequential execution prevents buildPath overlap issues (per Research Pitfall 2).

**Step 2: Update package.json exports map (per Research Pitfall 6)**

Add 4 new export entries to `packages/tokens/package.json`. The exports object must become:

```json
"exports": {
  ".": {
    "types": "./dist/index.d.ts",
    "import": "./dist/index.js",
    "require": "./dist/index.cjs"
  },
  "./css": "./dist/css/tokens.css",
  "./parent-brand/light": "./dist/parent-brand/light.css",
  "./parent-brand/dark": "./dist/parent-brand/dark.css",
  "./child-brand/light": "./dist/child-brand/light.css",
  "./child-brand/dark": "./dist/child-brand/dark.css"
}
```

**Step 3: Update turbo.json build:tokens outputs**

Update the `build:tokens` task outputs array to include the new CSS paths:

```json
"build:tokens": {
  "outputs": ["dist/css/**", "dist/parent-brand/**", "dist/child-brand/**"],
  "inputs": [
    "tokens/**/*.tokens.json",
    "style-dictionary.config.mjs",
    "package.json"
  ]
}
```

**Step 4: Run build and validate**

Run `cd packages/tokens && node style-dictionary.config.mjs` and verify:
- `dist/css/tokens.css` still exists (primitive vars, backwards compat)
- `dist/parent-brand/light.css` exists with `:root` selector
- `dist/parent-brand/dark.css` exists with `[data-theme="dark"]` selector
- `dist/child-brand/light.css` exists with `:root` selector
- `dist/child-brand/dark.css` exists with `[data-theme="dark"]` selector
- Semantic CSS files contain `var(--dsx-color-*)` references (outputReferences working)
- Semantic CSS files do NOT contain primitive token declarations (include vs source working correctly)
- `src/breakpoints.ts` still generated

Then run `turbo run build` to verify full pipeline.
  </action>
  <verify>
    <automated>cd "/Users/merlinzuni/develop/design system tokens" && cd packages/tokens && node style-dictionary.config.mjs && test -f dist/css/tokens.css && test -f dist/parent-brand/light.css && test -f dist/parent-brand/dark.css && test -f dist/child-brand/light.css && test -f dist/child-brand/dark.css && grep -q "var(--dsx-color-" dist/parent-brand/light.css && grep -q ':root' dist/parent-brand/light.css && grep -q 'data-theme="dark"' dist/parent-brand/dark.css && grep -q "parent-brand/light" package.json && cd ../.. && npx turbo run build 2>&1 | tail -5 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - style-dictionary.config.mjs contains `const BRANDS = ['parent-brand', 'child-brand']`
    - style-dictionary.config.mjs contains `const MODES = ['light', 'dark']`
    - style-dictionary.config.mjs contains `for (const brand of BRANDS)` nested loop
    - style-dictionary.config.mjs contains `include: ['tokens/primitives/**/*.tokens.json']` in semantic instances
    - `node style-dictionary.config.mjs` exits 0 with no errors
    - dist/parent-brand/light.css contains `:root {` and `--dsx-color-background-default`
    - dist/parent-brand/dark.css contains `[data-theme="dark"]` and `--dsx-color-background-default`
    - dist/parent-brand/light.css contains `var(--dsx-color-` (outputReferences preserved)
    - dist/parent-brand/light.css does NOT contain `--dsx-color-neutral-` (primitives excluded from semantic output)
    - dist/css/tokens.css still exists and contains `--dsx-color-neutral-`
    - package.json exports contains `"./parent-brand/light"`, `"./parent-brand/dark"`, `"./child-brand/light"`, `"./child-brand/dark"`
    - turbo.json build:tokens outputs contains `"dist/parent-brand/**"` and `"dist/child-brand/**"`
    - `turbo run build` exits 0
  </acceptance_criteria>
  <done>SD multi-instance loop produces 4 semantic CSS files + primitive CSS. Package exports and turbo outputs updated. Full build pipeline passes. Semantic CSS uses var() references to primitive CSS vars.</done>
</task>

</tasks>

<verification>
1. `cd packages/tokens && node style-dictionary.config.mjs` exits 0 — all 5 SD instances (1 primitive + 4 semantic) build without errors
2. `ls dist/parent-brand/light.css dist/parent-brand/dark.css dist/child-brand/light.css dist/child-brand/dark.css dist/css/tokens.css` — all 5 CSS files exist
3. `grep -c "var(--dsx-" dist/parent-brand/light.css` — semantic vars use outputReferences
4. `grep -c "^  --dsx-color-neutral-" dist/parent-brand/light.css` returns 0 — primitives NOT in semantic output
5. `turbo run build` exits 0 — full pipeline passes
6. Every `$value` in semantic JSON files is an alias (no raw hex)
</verification>

<success_criteria>
- 4 semantic CSS files generated from 4 semantic JSON source files
- SD config runs one instance per brand x mode combination (TOKEN-10)
- Semantic files define light (TOKEN-06) and dark (TOKEN-07) mode aliases
- Multi-brand support with parent-brand and child-brand (TOKEN-08)
- All alias chains preserved as var() references in CSS output
- Primitive CSS still builds correctly alongside semantic outputs
- Package exports and turbo.json updated for downstream consumers
</success_criteria>

<output>
After completion, create `.planning/phases/03-semantic-tokens-figma-pipeline/03-01-SUMMARY.md`
</output>
